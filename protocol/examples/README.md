# Protocol-only synthetic examples

These small records exercise the envelope shapes, reference hashes, and request/result binding only.
The `urn:keensight:protocol-fixture:*` payloads are deliberately not labeled as production collector or canonical Fact schemas. They are not valid scan/admission fixtures and cannot activate any live module.
All input hashes resolve to the illustrative bytes in `fixture_records.json`. Authentication and policies in the example are placeholders, not authorization.
