# Assembly provenance

The collector/canonical/protocol/product-design files came from KeenSight_E2E_Review_and_Repair_2026-09-15.zip. The baseline directory in both later audit archives was compared byte-for-byte and was identical to that repair archive (439 packaged entries). The component-source manifest covers the 311 files belonging to the four active component directories.

The later audit packages are imported without duplicate baseline trees. Their path defaults point to the root source. The root walkthrough harness receives that same source explicitly. No application algorithm, validator, contract or test inside those four component directories was changed in this assembly.

Historical narrative, source excerpts, audit observations, and earlier reports are retained separately. New root documentation does not convert designs into runtime features. The originally supplied business ledger and workbook are copied without changing their content or treating the ledger's claims as independently verified.

See IMPORTS.json for archive SHA-256 digests, original member names and exact four path adaptations. SOURCE_MANIFEST.json at repository root hashes the final delivered files; it is an integrity inventory, not an authenticity signature.
